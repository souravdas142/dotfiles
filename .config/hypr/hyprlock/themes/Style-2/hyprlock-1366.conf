# BACKGROUND
background {
    monitor =
    path = ~/.config/hypr/hyprlock/themes/Style-2/hypr.png
    blur_passes = 0
    contrast = 0.8916
    brightness = 0.8172
    vibrancy = 0.1696
    vibrancy_darkness = 0.0
}

# GENERAL
general {
    no_fade_in = false
    grace = 0
    disable_loading_bar = false
}

# TIME
label {
    monitor =
    text = cmd[update:1000] echo "<span>$(date +"%I:%M")</span>"
    color = rgba(216, 222, 233, .7)
    font_size = 120                    # Adjusted font size for smaller resolution
    font_family = steelfish outline regular
    position = 0, 240                  # Centered within the smaller screen
    halign = center
    valign = center
}

# DAY-MONTH-DATE
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date +"%A, %B %d")"
    color = rgba(216, 222, 233, .7)
    font_size = 22                    # Adjusted font size
    font_family = SF Pro Display Bold
    position = 0, 320                 # Aligned below the time
    halign = center
    valign = center
}

# FOREGROUND IMAGE
image {
    monitor =
    path = ~/.config/hypr/hyprlock/themes/Style-2/foreground.png
    size = 500                       # Adjusted image size for full visibility
    border_size = 0
    rounding = 0
    rotate = 0
    reload_time = 0
    reload_cmd = 
    position = 0, -50                  # Centered horizontally and vertically
    halign = center
    valign = center
}

# USER
label {
    monitor =
    text =     $USER
    color = rgba(216, 222, 233, 0.80)
    outline_thickness = 2
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    font_size = 16                  # Adjusted font size
    font_family = SF Pro Display Bold
    position = 0, -150              # Positioned slightly above the input field
    halign = center
    valign = center
}

# INPUT FIELD
input-field {
    monitor =
    size = 250, 50                 # Adjusted size for smaller resolution
    outline_thickness = 2
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgba(255, 255, 255, 0)
    inner_color = rgba(255, 255, 255, 0.1)
    font_color = rgb(200, 200, 200)
    fade_on_empty = false
    font_family = SF Pro Display Bold
    placeholder_text = <i><span foreground="##ffffff99">🔒 Enter Pass</span></i>
    hide_input = false
    position = 0, -220             # Centered vertically with respect to input
    halign = center
    valign = center
}

